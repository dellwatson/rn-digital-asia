import React from 'react'
import { View, Text } from 'react-native'

export default () =>
    <View style={{
        width: 30, height: 30, borderWidth: 1, borderColor: 'lightgrey',
        borderRadius: 50
    }} />
